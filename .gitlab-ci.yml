
stages: 
    # - build
    - build
    

test-job:
  image: mcr.microsoft.com/playwright
  stage: build
  script:
      - npm ci
      - npx playwright install --with-deps
      - npx playwright test
  artifacts:
     when: always
     paths: 
       - playwright-report/**
      # - echo: "Running tests."
      # - sleep  60
      # - echo: "Code coverage is 90%."

      



